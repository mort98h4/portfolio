---
interface Props {
    id: string;
    classList?: string;
}

const { id, classList = '' } = Astro.props;
const classes = classList + ' pt-12 print:pt-0 print:pb-4' 
---

<section id={id} class={classes}>
    <slot />
</section>